<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="false"
        android:key="timer"
        android:summary="@string/pref_enable_timed_mode_description"
        android:title="@string/pref_enable_timed_mode" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_difficulty_list"
        android:entryValues="@array/pref_difficulty_list_values"
        android:key="difficulty"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_difficulty" />

</PreferenceScreen>
